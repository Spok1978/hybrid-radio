# 28. Образцы экранов интерфейса

Нарисованы 2026-08-26 по спецификации [26](26-firmware-spec.md) — как проверка
того, что задание понято правильно. **Приняты заказчиком как рабочий вариант**
в тот же день.

## Где лежат

| Что | Где |
|---|---|
| Исходники экранов | `design/*.dc.html`, по файлу на экран |
| Сборщик страницы | `design/` собирается скриптом, вклеивающим экраны в общую страницу |
| Собранная страница | `design/ip-radio-screens.html` |
| Опубликованный вид | https://claude.ai/code/artifact/f56b7200-34dc-466d-b2f4-98e7662892f4 |

Экраны нарисованы в натуральном кадре **1280×720** — в том виде, в каком они
окажутся на панели после поворота ([26](26-firmware-spec.md), §2.1).

## Двенадцать состояний

| № | Экран | Что демонстрирует |
|---|---|---|
| 01 | Эфир, станция с RDS | название из RDS крупно, частота под ним, активен пресет типа FM |
| 02 | Эфир, станция без RDS | названия нет — крупно частота; второй диапазон |
| 03 | Интернет-радио, играет | метаданные ICY, кодек, заполнение буфера |
| 04 | Интернет-радио, буферизация | переход по пресету другого типа: на экране написано, что происходит |
| 05 | Меню настроек | крупные строки, текущее значение справа у каждой |
| 06 | Настройка эфира | шкала с засечками автопоиска, переключатель диапазона |
| 07 | Экранная клавиатура | ЙЦУКЕН, клавиша выбрана энкодером, «Сохранить» отдельно |
| 08 | Ждущий режим | часы во весь экран, приглушено, внизу что играет |
| 09 | Звук выключен | красный перечёркнутый динамик, зачёркнутая громкость, плашка по центру |
| 10 | Wi-Fi не настроен | приглашение настроить сеть с прямым переходом, эфир играет |
| 11 | Сеть настроена, связи нет | предупреждение поверх эфира, идёт переподключение |
| 12 | Станция не отвечает | дело в станции, а не в сети; сразу предложены другие |

## Что из этого уже написано

Состояние на 2026-08-27. Собирается, на железе не проверено — платы нет.

| № | Экран | Модуль | Состояние |
|---|---|---|---|
| 01–04 | воспроизведение, оба режима | `ipradio_ui.c` | **готов** |
| 05 | меню настроек | `ipradio_ui_menu.c` | **готов** |
| 06 | настройка эфира | `ipradio_ui_tune.c` | **готов**, кроме самого прохода автопоиска |
| 07 | экранная клавиатура | `ipradio_ui_keyboard.c` | **готова**, открывать пока некому |
| 08 | ждущий режим | `ipradio_ui_idle.c` | **готов** |
| 09 | звук выключен | `ipradio_ui.c` | **готов** (плашка на главном экране) |
| 10–12 | недоступность сети | `ipradio_ui_dialog.c` | **готовы** |

Чего ещё нет и что мешает считать интерфейс законченным:

1. **Выбор сети Wi-Fi** — экран со списком найденных сетей. Без него
   клавиатура не открывается ни разу: кнопка «Настроить Wi-Fi»
   в диалоге 10 пишет в журнал.
2. **Список станций** — переименование, удаление, правка пресетов
   (§5.3.3.4). Второе место, откуда открывалась бы клавиатура.
3. **Поиск по каталогу** интернет-станций (§5.3.2).
4. **Состояние прибора** — экран диагностики (§5.3.3.5).
5. **Проход автопоиска** по диапазону. Экран 06 умеет показывать
   засечки, но никто не зовёт `ipradio_tune_add_mark()`: сам проход —
   дело модуля тюнера, он ещё не написан.

### Как устроен перехват органов управления

Экранов, забирающих себе энкодеры, стало пять, и это отдельная
механика, а не свойство каждого из них.

Автомат состояния о экранах не знает — он знает про радио. Перехват
принадлежит интерфейсу и сделан **фильтром событий**
(`ipradio_set_event_filter`): фильтр ставится один раз при подъёме
и висит всегда, решая по обстановке. Ставить и снимать его при каждом
диалоге значило бы завести ещё одно состояние, которое можно
рассинхронизировать.

Порядок наложения — от верхнего к нижнему: клавиатура, настройка эфира,
меню, диалог. Он же порядок проверок в разборе команд.

Что фильтр **не** перехватывает никогда: громкость, звук и питание.
Прибор не должен становиться неуправляемым из-за плашки на экране.

Фильтр работает в задаче автомата, а виджеты LVGL можно трогать только
из задачи отрисовки. Поэтому он не рисует, а кладёт команду в короткое
кольцо, которое разбирает задача интерфейса.

---

## Решения, принятые при отрисовке

**Цвет кодирует режим.** Янтарь — эфир, циан — интернет. Не украшение:
режим читается раньше, чем прочитан текст — по бейджу справа вверху,
по подсветке активного пресета, по цвету частоты. Для пользователя, ради
которого заведено требование R4.1, это работает лучше подписи.

**Названия диапазонов на экране короткие — «УКВ» и «FM».** Решение заказчика
2026-08-26 по итогам просмотра. OIRT и CCIR остаются в документации:
пользователю они ничего не говорят, а место занимают. Внесено
в [26](26-firmware-spec.md), §5.1.

**Уровень громкости числом в статусной строке.** В ТЗ этого не было. Раз
энкодер 2 крутит громкость вслепую, цифру полезно видеть — тем более что
в эфире ступеней всего 16 ([23](23-volume-control.md)).

**В меню у каждой строки показано текущее значение** — «NTP, UTC+3», «80 %»,
«34 сохранено». Чтобы проверить настройку, не надо заходить внутрь пункта.

**Шрифт IBM Plex Sans и IBM Plex Mono.** Материал-дизайн предполагает Roboto,
но у Plex Mono табличные цифры: частота не дёргается при перестройке, когда
меняется разряд. На приборе это заметно. Окончательный выбор шрифта делается
при генерации кириллического шрифта для LVGL ([26](26-firmware-spec.md), §7).

**Mute показан крупно, а не только значком.** Заказчик просил красный
перечёркнутый динамик — он сделан, но к нему добавлена плашка «ЗВУК ВЫКЛЮЧЕН»
по центру. Значок 22 пикселя в углу для пожилого пользователя мал: человек
решит, что прибор сломался, а не что он сам выключил звук.

**Недоступность интернета показана до нажатия, а не после.** NET-ячейки
гаснут и получают перечёркнутый Wi-Fi, когда связи нет.

**Три состояния недоступности разведены** — уточнение заказчика 2026-08-26.
Переключение в интернет-режим блокируется, но реакция на MODE разная: если
сеть не настроена, прибор **приглашает её настроить** и ведёт прямо в
настройку; если настроена, но связи нет — просто предупреждает и никуда не
ведёт, потому что связь вернётся сама. Третий случай, когда сеть есть,
а станция молчит, отделён специально: иначе человек будет чинить Wi-Fi,
который исправен. Разбор — [26](26-firmware-spec.md), §5.2.

**Диалоги показываются поверх играющего эфира**, а не вместо него, и на
каждом написано, что эфир не прерывался. Это сознательно: молчащий прибор
с сообщением об ошибке читается как «сломалось всё».

## Оговорки

- Названия станций в пресетах — правдоподобные примеры, не реальные данные.
- Экраны нарисованы вёрсткой, а не в LVGL. Это макет соглашений и раскладки,
  а не проверка того, что LVGL нарисует то же самое с той же скоростью.
- Канву дизайн-редактора собрать не удалось: для неё нужен Node, а на машине
  его нет. Поэтому обычная страница; править экраны можно поштучно,
  исходники разделены по файлам.
