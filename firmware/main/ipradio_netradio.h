/*
 * ipradio_netradio.h — приём и воспроизведение интернет-потока.
 *
 * Построено на компонентах ESP-ADF: HTTP-поток, выбор декодера по типу
 * и вывод на I²S. Выбор ADF обоснован в docs/27-reference-sheet.md, §1.1;
 * опасение насчёт конфликта версий с esp_hosted снято — ни один
 * компонент ADF его не требует (docs/26-firmware-spec.md, §2.4).
 *
 * Почему именно «выбор декодера по типу», а не фиксированный MP3:
 * станции приходят в разных кодеках, и угадывать нельзя — часть
 * попросту не заиграет.
 */

#pragma once

#include <stdbool.h>
#include <stdint.h>

#include "esp_err.h"

#ifdef __cplusplus
extern "C" {
#endif

/** Поднять конвейер. Ничего не играет, пока не позовут play. */
esp_err_t ipradio_netradio_init(void);

/** Начать проигрывание потока по адресу.
 *  Возврат немедленный: подключение и наполнение буфера идут фоном,
 *  а состояние приезжает в автомат событиями. */
esp_err_t ipradio_netradio_play(const char *url, const char *station_name);

/** Остановить. Конвейер остаётся поднятым: следующий запуск будет
 *  быстрее, а задача плеера не выгружается при уходе в эфир (§5.2). */
esp_err_t ipradio_netradio_stop(void);

/** Играет ли что-нибудь прямо сейчас. */
bool ipradio_netradio_active(void);

/** Громкость интернет-потока.
 *
 *  Делается регулятором ALC внутри конвейера, а не умножением
 *  отсчётов в обход: отсчётами владеет ADF, и лезть в его буферы
 *  значит спорить с ним за одни и те же данные.
 *
 *  @param logical  0…100, та же шкала, что у автомата
 *  @param mute     заглушить независимо от уровня */
void ipradio_netradio_set_volume(uint8_t logical, bool mute);

#ifdef __cplusplus
}
#endif
