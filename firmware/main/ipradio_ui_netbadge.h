/*
 * ipradio_ui_netbadge.h — состояние сети, видное на любом экране.
 *
 * Требование заказчика 2026-08-30: значок Wi-Fi должен быть на КАЖДОМ
 * экране. Причина понятная: почти всё непонятное поведение прибора
 * упирается в сеть, и человек не должен ради этого куда-то заходить.
 *
 * Отсюда устройство. Экраны в этой прошивке — полноэкранные объекты,
 * которые перекрывают друг друга; повесить значок на каждый значило бы
 * завести двенадцать копий, которые рано или поздно разойдутся.
 * Поэтому он живёт на ВЕРХНЕМ СЛОЕ LVGL (`lv_layer_top`), который
 * рисуется поверх всего и ничьим экраном не закрывается.
 *
 * Значка как картинки нет: в нашем урезанном шрифте нет ни одного
 * подходящего символа, а тащить набор пиктограмм ради одной иконки
 * незачем. Вместо неё цветная точка и слово — точка видна издалека,
 * слово объясняет, что именно не так.
 */

#pragma once

#include "esp_err.h"
#include "ipradio_state.h"

#ifdef __cplusplus
extern "C" {
#endif

/** Создать на верхнем слое. Звать один раз, при подъёме интерфейса. */
esp_err_t ipradio_netbadge_init(void);

/** Обновить по снимку состояния. */
void ipradio_netbadge_update(const ipradio_snapshot_t *snap);

#ifdef __cplusplus
}
#endif
