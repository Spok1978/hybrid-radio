/*
 * ipradio_input.h — регуляторы и кнопки.
 *
 * Единственный модуль, который превращает действия человека в события
 * автомата. Требования — docs/26-firmware-spec.md, §6.1, и R4.1:
 * любой сценарий проходим физическими органами, без касания экрана.
 *
 * Различает короткое, двойное и длинное нажатие. Тайминги — из §6.1,
 * вынесены в board_pins.h и уточняются на макете.
 */

#pragma once

#include "esp_err.h"

#ifdef __cplusplus
extern "C" {
#endif

/** Поднять опрос органов управления и его задачу.
 *  Вызывать ПОСЛЕ ipradio_state_init(): модуль сразу начнёт слать
 *  события в автомат. */
esp_err_t ipradio_input_init(void);

/** Сколько миллисекунд прошло с последнего действия человека.
 *  Нужно ждущему режиму: вход через три минуты бездействия (§5.4). */
uint32_t ipradio_input_idle_ms(void);

#ifdef __cplusplus
}
#endif
