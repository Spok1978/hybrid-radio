/*
 * ipradio_bridge.h — перенос состояния на железо.
 *
 * Автомат (ipradio_state) хранит, ЧТО должно происходить. Тюнер,
 * звуковой тракт и конвейер потока умеют это делать. Между ними
 * не было ничего, кроме громкости, и это была главная незаделанная
 * дырка: автомат честно менял частоту в своей структуре, а чип
 * оставался на прежней.
 *
 * Мост подписывается на автомат и после каждого события сверяет
 * состояние с тем, что уже применено. Отсюда его устройство: он
 * помнит последнее применённое и трогает железо только на разнице.
 *
 * Почему сверка, а не команды. Событий много, и каждое могло бы
 * само дёргать нужный узел — но тогда любое новое событие требовало бы
 * помнить весь список железа, которого оно касается. Сверка снимает
 * этот вопрос: событий может быть сколько угодно, состояние одно.
 *
 * Мост — единственное место, которое знает и про автомат, и про
 * железо сразу. Ни тюнер, ни конвейер друг о друге не знают.
 */

#pragma once

#include "esp_err.h"

#ifdef __cplusplus
extern "C" {
#endif

/** Подписаться на автомат. Звать после того, как подняты тюнер,
 *  звуковой тракт и конвейер: мост начнёт их дёргать сразу. */
esp_err_t ipradio_bridge_init(void);

/** Разовая проверка при старте, §5.2, правило 4.
 *
 *  Звать по секундному тику, пока не вернёт true. Решает, что делать,
 *  если человек выключился в интернет-режиме: дождаться сети
 *  и вернуть его станцию, либо через пятнадцать секунд оставить эфир
 *  и написать об этом.
 *
 *  Радио, молчащее после включения, выглядит неисправным - поэтому
 *  решение принимается само. Но однократно: переигрывать его, когда
 *  сеть появится позже, нельзя - оно уже показано человеку. */
bool ipradio_bridge_boot_check(void);

#ifdef __cplusplus
}
#endif
